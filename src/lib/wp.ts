/* interface pageData {
  slug: string;
  title: string;
  content: string;
  image: string | null;
}

export const getPageContent = async (
  pagesData: pageData[]
): Promise<pageData | null> => {
  const pageContent = pagesData.find((p) => p.slug === 'hero');
  return pageContent || null;
};
 */
